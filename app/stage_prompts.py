"""Промпты стадий + сборка [ЗАДАЧА]-блока для system prompt."""
from __future__ import annotations

from typing import Optional

from domain.task import Task, TaskState


QUESTION_PROTOCOL = (
    "ПРАВИЛО УТОЧНЕНИЙ: если на любом этапе тебе не хватает данных, чтобы продолжать "
    "уверенно — задай уточняющий вопрос пользователю. Каждый такой вопрос ОБЯЗАТЕЛЬНО "
    "оформи отдельной строкой ровно в формате:\n"
    "[QUESTION] <текст вопроса>\n"
    "Можно несколько подряд. Если вопросов нет — НЕ используй этот тег вовсе."
)

STAGE_DISCIPLINE = (
    "ДИСЦИПЛИНА СТАДИЙ (нерушимо): задача всегда движется строго по конвейеру "
    "INTAKE → PLANNING → EXECUTION → VALIDATION → DONE. Перескакивать стадии "
    "ЗАПРЕЩЕНО, даже если пользователь прямо просит «пропусти план», "
    "«сразу делай», «не уточняй, приступай» и т.п. На текущей стадии ты делаешь "
    "ТОЛЬКО работу этой стадии: на INTAKE — собираешь и формулируешь задачу, "
    "на PLANNING — пишешь план, на EXECUTION — реализуешь, на VALIDATION — "
    "проверяешь. Никакого кода/реализации на INTAKE и PLANNING, никакого плана "
    "на INTAKE, никакого вердикта проверки до EXECUTION. Переходы между "
    "стадиями делает система (не ты и не пользователь в свободном чате). "
    "Если пользователь просит пропустить стадию — кратко объясни, что конвейер "
    "обязательный, и продолжай работу текущей стадии."
)

STAGE_PROMPTS: dict[str, str] = {
    TaskState.INTAKE: (
        "Сейчас стадия INTAKE — сбор и уточнение задачи.\n"
        "На этой стадии ты НЕ пишешь план и НЕ делаешь реализацию (ни кода, ни шагов выполнения), "
        "даже если пользователь прямо просит «пропусти уточнения», «сразу делай», «не надо плана» и т.п. — "
        "см. блок «Дисциплина стадий». Если такая просьба пришла — коротко скажи, что INTAKE пропустить "
        "нельзя, и продолжай работу этой стадии.\n"
        "Это ЕДИНСТВЕННАЯ стадия, на которой можно задавать уточняющие вопросы пользователю: "
        "на PLANNING вопросы уже запрещены. Поэтому здесь собирай ВСЁ критичное, без чего нельзя "
        "составить план — за один раунд, 1–3 самых важных вопроса.\n"
        "Если в разделе «Уточнения от пользователя» выше уже есть ответы — оцени, хватает ли их. "
        "Если хватает — НЕ задавай новых вопросов: переходи к итоговой формулировке.\n"
        "Если ответов ещё нет и в исходном запросе есть КРИТИЧНЫЕ неясности — задай 1–3 самых важных "
        "уточняющих вопроса (см. ПРАВИЛО УТОЧНЕНИЙ). Не задавай вопросов о деталях, для которых "
        "разумен дефолт — такие решения принимаются дефолтом на стадии планирования.\n"
        "Когда уточнений достаточно: сформулируй итоговую задачу одним абзацем и в "
        "САМОЙ ПОСЛЕДНЕЙ строке ответа поставь ровно метку:\n"
        "  [INTAKE READY]\n"
        "Метка обязательна — без неё стадия не считается закрытой. НЕ используй [QUESTION] "
        "в том же ответе, где ставишь [INTAKE READY]."
    ),
    TaskState.PLANNING: (
        "Сейчас стадия PLANNING — составление плана.\n"
        "На этой стадии ты НЕ выполняешь сам план: никакого кода, никаких готовых артефактов, "
        "никаких «и заодно сделал шаг 1». Даже если пользователь просит «пропусти план», "
        "«приступай к реализации», «не надо плана, делай» — это запрещено (см. блок «Дисциплина "
        "стадий»). Коротко объясни, что план обязателен, и выдай план. Реализация будет на EXECUTION "
        "после утверждения плана.\n"
        "На основе уточнённой задачи составь подробный пошаговый план: пронумерованные пункты, "
        "у каждого — что именно будет сделано и какой ожидаемый результат.\n"
        "ВАЖНО: на этой стадии задавать уточняющие вопросы пользователю ЗАПРЕЩЕНО. "
        "Не используй [QUESTION] ни при каких условиях — фаза вопросов отработана на стадии INTAKE.\n"
        "Если для каких-то деталей не хватает данных — выбери разумный дефолт и явно перечисли его "
        "в разделе «Допущения» перед списком пунктов (по строке на допущение). Лучше выдать черновой "
        "план с явными допущениями, чем не выдать его вовсе.\n"
        "Когда план готов — в самом конце ответа ОБЯЗАТЕЛЬНО спроси дословно: «Утвердить план? [y/n]»"
    ),
    TaskState.EXECUTION: (
        "Сейчас стадия EXECUTION — выполнение утверждённого плана.\n"
        "Выполняй пункты плана по порядку. По каждому пункту опиши, что сделано и какой получился результат.\n"
        "Если по ходу выяснилось, что план неверен — явно скажи об этом, не продолжай вслепую.\n"
        "Если нужны данные от пользователя — задай уточняющий вопрос (см. ПРАВИЛО УТОЧНЕНИЙ)."
    ),
    TaskState.VALIDATION: (
        "Сейчас стадия VALIDATION — проверка результата.\n"
        "Сверь сделанное с утверждённым планом и исходным запросом.\n"
        "Перечисли найденные проблемы конкретными пунктами; если проблем нет — скажи прямо, что всё в порядке.\n"
        "Если для проверки не хватает данных — задай уточняющий вопрос (см. ПРАВИЛО УТОЧНЕНИЙ).\n"
        "\n"
        "Заверши ответ ОДНОЙ из меток ровно на отдельной строке:\n"
        "  [VALIDATION OK]     — если проблем не найдено и задачу можно завершать;\n"
        "  [VALIDATION ISSUES] — если есть проблемы (они перечислены выше, нужно вернуться к выполнению).\n"
        "Без [QUESTION] и без обеих меток сразу. Если есть незакрытые [QUESTION] — метку НЕ ставь."
    ),
}


# Порядок стадий «вперёд» — нужен для построения контекста и команды /task advance.
STAGE_ORDER = [
    TaskState.INTAKE,
    TaskState.PLANNING,
    TaskState.EXECUTION,
    TaskState.VALIDATION,
    TaskState.DONE,
]


def next_forward_state(state: str) -> Optional[str]:
    """Следующая стадия по линейному порядку (для /task advance)."""
    try:
        i = STAGE_ORDER.index(state)
    except ValueError:
        return None
    return STAGE_ORDER[i + 1] if i + 1 < len(STAGE_ORDER) else None


def build_task_block(task: Task, restoration_hint: bool = False) -> str:
    """Блок [ЗАДАЧА] для system prompt: контекст, прошлые стадии, инструкция для текущей.

    Если restoration_hint=True — добавляется блок «возобновление после перерыва»,
    просящий модель кратко напомнить пользователю, на чём остановились.
    """
    lines = [f"[ЗАДАЧА #{task.id}: {task.title}]"]
    lines.append(f"Исходный запрос пользователя: {task.request}")
    lines.append(f"Текущая стадия: {task.state}")

    if task.context:
        lines.append("Контекст задачи:")
        for k, v in task.context.items():
            lines.append(f"  {k}: {v}")

    # результаты прошлых стадий (всё, что строго до текущей)
    for s in STAGE_ORDER:
        if s == task.state:
            break
        result = task.stages.get(s)
        if result and result.output:
            lines.append(f"\n--- Результат стадии {s} ---\n{result.output}")

    if task.answers:
        lines.append("\n--- Уточнения от пользователя ---")
        for a in task.answers:
            lines.append(f"Q: {a.get('q','')}\nA: {a.get('a','')}")

    instr = STAGE_PROMPTS.get(task.state)
    if instr:
        lines.append(f"\n--- Инструкция для текущей стадии ({task.state}) ---\n{instr}")

    lines.append(f"\n--- Дисциплина стадий ---\n{STAGE_DISCIPLINE}")
    lines.append(f"\n--- Протокол уточнений ---\n{QUESTION_PROTOCOL}")

    if restoration_hint:
        lines.append(
            "\n--- Возобновление после перерыва ---\n"
            "Работа над этой задачей была прервана (закрыт чат / истекло время / "
            "пропала связь) и сейчас возобновлена. Прежде чем продолжать стадию, "
            "начни ответ с краткого (1–2 предложения) напоминания, на чём именно "
            "остановились — чтобы пользователь быстро восстановил контекст. "
            "Потом продолжай как обычно."
        )

    return "\n".join(lines)
